// Example JavaScript to update the win and loss values
function updateStats(wins, losses) {
    document.getElementById('wins-sidebar').textContent = wins;
    document.getElementById('losses-sidebar').textContent = losses;
}
