import hvac

VAULT_HOSTNAME = 'http://vault'
VAULT_PORT = 8200
VAULT_TOKEN = 'xxx'

VAULT_CLIENT = hvac.Client(
    url=f'{VAULT_HOSTNAME}:{VAULT_PORT}',
    token=VAULT_TOKEN,
)

VAULT_MOUNTPOINT = 'kv'

